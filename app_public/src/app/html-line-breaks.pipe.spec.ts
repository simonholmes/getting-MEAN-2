import { HtmlLineBreaksPipe } from './html-line-breaks.pipe';

describe('HtmlLineBreaksPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlLineBreaksPipe();
    expect(pipe).toBeTruthy();
  });
});
