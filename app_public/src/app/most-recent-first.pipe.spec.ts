import { MostRecentFirstPipe } from './most-recent-first.pipe';

describe('MostRecentFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new MostRecentFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
